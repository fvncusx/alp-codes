let n1 = Number(prompt("Digite um número inteiro (positivo ou negativo, para negativo use "-"):"));
if(n1>0){
    document.write(`${n1} é um número positivo, ou seja, é maior que 0.`)
} else{
    document.write(`${n1} é um número negativo, ou seja, é menor que 0.`)
}